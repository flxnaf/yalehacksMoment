# Xcode
*.xcuserstate
xcuserdata/
*.xccheckout
*.moved-aside
DerivedData/
*.hmap
*.ipa
*.dSYM.zip
*.dSYM

# Swift Package Manager
.build/
*.resolved
!NavigatorImpaired/NavigatorImpaired.xcodeproj/project.xcworkspace/xcshareddata/swiftpm/Package.resolved

# CocoaPods
Pods/

# macOS
.DS_Store
.AppleDouble
.LSOverride

# CoreML compiled cache
**/*.mlmodelc

# Hugging Face CLI cache — must not live under the app’s synchronized folder or Xcode
# compiles the same .mlpackage twice (“Multiple commands produce”).
NavigatorImpaired/NavigatorImpaired/.cache/

# Local Hugging Face cache for setup_model.sh
.hf-home/

# Meta SDK — resolved automatically by Xcode SPM
meta-wearables-dat-ios/

# VisionClaw — real API keys (never commit). Template: VisionClaw/Secrets.swift.example
NavigatorImpaired/NavigatorImpaired/VisionClaw/Secrets.swift
